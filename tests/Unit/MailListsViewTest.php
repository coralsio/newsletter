<?php

namespace Tests\Feature;

use Corals\User\Models\User;
use Illuminate\Foundation\Testing\DatabaseTransactions;
use Illuminate\Support\Facades\Auth;
use Tests\TestCase;

class MailListsViewTest extends TestCase
{
    use DatabaseTransactions;

    protected function setUp(): void
    {
        parent::setUp(); // TODO: Change the autogenerated stub

        $user = User::query()->whereHas('roles', function ($query) {
            $query->where('name', 'superuser');
        })->first();
        Auth::loginUsingId($user->id);
    }

    /**
     * A basic test example.
     *
     * @return void
     */
    public function test_mail_lists_view()
    {
        $response = $this->get('newsletter/mail-lists');

        $response->assertStatus(200)->assertViewIs('Newsletter::mail_lists.index');
    }

    public function test_mail_lists_create()
    {
        $response = $this->get('newsletter/mail-lists/create');

        $response->assertViewIs('Newsletter::mail_lists.create_edit')->assertStatus(200);
    }
}
